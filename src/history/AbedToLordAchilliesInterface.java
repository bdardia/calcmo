package history;

import java.util.ArrayList;

public interface AbedToLordAchilliesInterface 
{
	///once you fill out the output simply add it to the arrayList the historyScreen owns
	AbedHistoryNode addToList(AbedHistoryNode a, ArrayList<AbedHistoryNode> s);
}
